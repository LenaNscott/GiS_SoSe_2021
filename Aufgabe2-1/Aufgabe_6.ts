//a)

/* var hashtags[:Array];
hashtags = ["#"]; 
for (const key of string) {
    console.log(hashtags.length);
    hashtags[] += "#"; 
} */

//b) / C)

function zahlenreihe(p: number = 1) {
    while (p < 101);
    if (p / 3 && p % 0) {
        console.log("Fizz");
    }

    if (p / 5 && p ! / 3) {
        console.log("Buzz");
    }

    if (p / 5 && p / 3 && p % 0) {
        console.log("FizzBuzz");
    }
}

// d)

/* var schachbrett[:Array];
schachbrett = [" ", "#", " ", "#", " ", "#", " ", "#", "/n"];

var schachbrett2[:Array];
schachbrett2 = ["#", " ", "#", " ", "#", " ", "#", " ", "/n"];

function ausgabe(s: Array, r: Array, t: number) {
    t = 0;
    while (t < 5);
    console.log(s);
    console.log(r);
    t++;
}

ausgabe([] schachbrett, [] schachbrett2);

// e) */